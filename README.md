# Gesture-Controlled Local Music Player

基于电脑摄像头的手势控制音乐播放器，使用 MediaPipe Hands 实时识别手势控制音乐播放。

## 系统要求

- Python 3.8 - 3.11
- Windows 10/11
- 电脑摄像头

## 快速开始

### 方法1：使用 requirements.txt

```bash
pip install -r requirements.txt
python main.py
```

### 方法2：使用 install.bat（Windows）

双击运行 `install.bat`

### 方法3：使用 setup.py

```bash
pip install -e .
python main.py
```

## 手势控制

| 手势 | 动作 |
|------|------|
| ✋ 张开手掌 | 播放 |
| ✊ 握拳 | 暂停 |
| 👍 竖起拇指 | 下一首 |
| 👎 拇指向下 | 上一首 |
| 🤘 摇滚手势 | 随机播放 |

## 目录结构

```
motion_music/
├── music/              # 音乐文件目录
├── hand_detector.py    # MediaPipe Hands 检测
├── gesture_classifier.py  # 手势识别
├── music_controller.py    # 音乐播放控制
├── main.py             # 主程序
├── requirements.txt    # 依赖列表
├── setup.py           # 安装配置
└── README.md          # 说明文档
```

## 故障排查

### 安装报错

如果遇到版本兼容性问题，尝试：

1. 使用 Python 3.8 或 3.9
2. 创建虚拟环境：
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### 摄像头无法打开

检查摄像头权限和是否有其他程序占用摄像头。

### MediaPipe 安装失败

在 Windows 上可能需要安装 Visual C++ 2015-2022 运行库。

### 音乐无法播放

确保音乐文件在 `music/` 目录下，支持 MP3/WAV/OGG 格式。

## 退出程序

按 `q` 键退出

